bits = [1, 0, 1, 0, 0, 0, 1]
print("bits is a list = ", bits)

print("how many 1's in bits? ", bits.count(1))
print("how many 2's in bits? ", bits.count(2))
print("how many 0's in bits? ", bits.count(0))
